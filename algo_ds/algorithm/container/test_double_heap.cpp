#include "catch.hpp"
#include "double_heap.hpp"

TEST_CASE("Test double heap", "")
{
    #if 0
    while (!std::cin.eof())
    {
        int tmp;
        std::cin >> tmp;
        if (tmp != 0 && tmp != -1)
        {
            post.push_back(tmp);
        }
        else if (tmp == -1)
        {
            balance();
            findMedium();
        }
        else
        {
            std::getline(std::cin, empty);
            std::cout << std::endl;
            clear();
        }
    }
    #endif
}